const input = prompt('N과 K를 공백으로 기준 입력').split(' ');

const arrA = prompt('공백 기준으로 입력').split(' ').sort();
const arrB = prompt('공백 기준으로 입력').split(' ').sort().reverse();

for (let i = 0; i < input[1]; i++) {

}